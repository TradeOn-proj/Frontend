const routes = {
  main: "/",
  home: "/home",
  value: "/value",
  upload: "/upload",
  valueupload: "/valueupload",
  detail: "/detail",
  valuedetail: "/valuedetail"
} as const;

export default routes;
