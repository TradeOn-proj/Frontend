const routes = {
  main: "/",
  home: "/home",
  search: "/search",
  searchresult: "/search-result",
  login: "/login",
  signin: "/signin",
  keyword: "/keywordpage",
} as const;

export default routes;
